{
  "name": "discord-drive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Discord-backed cloud drive with chunked uploads, OAuth2 login, and a web dashboard.",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node src/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
